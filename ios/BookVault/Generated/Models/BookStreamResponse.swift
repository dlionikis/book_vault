//
// BookStreamResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct BookStreamResponse: Sendable, Codable, JSONEncodable, Hashable {

    public enum Status: String, Sendable, Codable, CaseIterable {
        case available = "available"
        case restoring = "restoring"
    }
    /** - available: streamUrl is ready to play immediately - restoring: audio is in the S3 Intelligent-Tiering Archive Access   tier; a restore was initiated (or is already in progress)  */
    public var status: Status
    /** Presigned S3 URL (or local /api/audio URL). Present when status=available. */
    public var streamUrl: String?
    /** When streamUrl expires. Present when status=available. */
    public var expiresAt: Date?
    public var bookId: UUID?
    /** Human-readable status. Present when status=restoring. */
    public var message: String?
    /** When the restore was requested. Present when status=restoring. */
    public var requestedAt: Date?
    /** Estimated restore completion (requestedAt + ~5h). Present when status=restoring. */
    public var estimatedCompletion: Date?

    public init(status: Status, streamUrl: String? = nil, expiresAt: Date? = nil, bookId: UUID? = nil, message: String? = nil, requestedAt: Date? = nil, estimatedCompletion: Date? = nil) {
        self.status = status
        self.streamUrl = streamUrl
        self.expiresAt = expiresAt
        self.bookId = bookId
        self.message = message
        self.requestedAt = requestedAt
        self.estimatedCompletion = estimatedCompletion
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
        case streamUrl
        case expiresAt
        case bookId
        case message
        case requestedAt
        case estimatedCompletion
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(streamUrl, forKey: .streamUrl)
        try container.encodeIfPresent(expiresAt, forKey: .expiresAt)
        try container.encodeIfPresent(bookId, forKey: .bookId)
        try container.encodeIfPresent(message, forKey: .message)
        try container.encodeIfPresent(requestedAt, forKey: .requestedAt)
        try container.encodeIfPresent(estimatedCompletion, forKey: .estimatedCompletion)
    }
}

