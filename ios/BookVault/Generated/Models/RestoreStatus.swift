//
// RestoreStatus.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct RestoreStatus: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case available = "available"
        case archived = "archived"
        case restoring = "restoring"
    }
    /** - available: audio is ready to stream - archived: in the Archive Access tier; no restore in flight - restoring: a restore is in progress (~3-5 hours)  */
    public var status: Status
    /** When the active restore was requested. Present when status=restoring. */
    public var requestedAt: Date?
    /** requestedAt + ~5h. Present when status=restoring. */
    public var estimatedCompletion: Date?
    /** When the most recent restore completed. Present when status=available. */
    public var completedAt: Date?
    /** Error from the most recent failed restore. Present when status=archived. */
    public var lastError: String?

    public init(status: Status, requestedAt: Date? = nil, estimatedCompletion: Date? = nil, completedAt: Date? = nil, lastError: String? = nil) {
        self.status = status
        self.requestedAt = requestedAt
        self.estimatedCompletion = estimatedCompletion
        self.completedAt = completedAt
        self.lastError = lastError
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
        case requestedAt
        case estimatedCompletion
        case completedAt
        case lastError
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(requestedAt, forKey: .requestedAt)
        try container.encodeIfPresent(estimatedCompletion, forKey: .estimatedCompletion)
        try container.encodeIfPresent(completedAt, forKey: .completedAt)
        try container.encodeIfPresent(lastError, forKey: .lastError)
    }
}

