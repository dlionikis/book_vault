{
  "$schema": "https://opencode.ai/config.json",
  "instructions": ["CLAUDE.md"],
  "default_agent": "agent",
  "permission": {
    "bash": {
      "*": "allow",
      "rm -rf *": "ask",
      "git commit *": "ask",
      "git push *": "ask",
      "git push": "ask",
      "npm run deploy *": "ask",
      "npm run deploy": "ask",
      "aws *": "ask"
    },
    "edit": "allow",
    "read": "allow",
    "external_directory": "deny"
  }
}
